[example]
localhost ansible_connection=local ansible_python_interpreter=python3

[example:vars]
ansible_user=runner
deploy_env=validation

keycloak_realm=changeme
keycloak_admin=changeme
keycloak_admin_pass=changeme

keycloak_auth_url=changeme

minio_admin_user=changeme
minio_admin_pass=changeme

hostname=changeme
s3_public_backend=s3.changeme
s3_backend=s3.changeme
domain_name=changeme
dns_challenge_api_token=changeme

admin_whitelisted_ip=changeme
timezone=changeme
pushover_url=changeme

main_user_name=changeme
main_user_email=changeme